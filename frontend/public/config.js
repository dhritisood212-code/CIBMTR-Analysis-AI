// Runtime config — edit this AFTER deploying, no rebuild needed.
// Set apiBase to your hosted backend URL to run live, e.g.:
//   window.__APP_CONFIG__ = { apiBase: "https://cibmtr-repro-api.onrender.com" };
// Leave apiBase empty ("") to run as a pure client-side DEMO (renders the bundled
// P-5297 example). If apiBase is set but the backend is unreachable, the app also
// falls back to demo mode.
window.__APP_CONFIG__ = { apiBase: "" };
